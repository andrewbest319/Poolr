export default function Home() {
  return (
    <main className="min-h-screen bg-black text-white flex flex-col">
      
      {/* NAVBAR */}
      <nav className="w-full flex justify-between items-center px-8 py-6 border-b border-white/10">
        <h1 className="text-xl font-semibold tracking-wide">Poolr</h1>
        <div className="flex gap-8 text-sm text-gray-300">
          <a href="#">Home</a>
          <a href="#">How It Works</a>
          <a href="#">Pricing</a>
          <a href="#">Live Scoreboards</a>
          <a href="#">Login</a>
        </div>
      </nav>

      {/* HERO SECTION */}
      <section className="flex flex-col items-center justify-center text-center flex-1 px-6">
        <h2 className="text-5xl md:text-6xl font-bold leading-tight max-w-4xl">
          Build Brackets. Compete. Win Pools.
        </h2>

        <p className="mt-6 text-gray-400 max-w-xl">
          Create private pools with friends, track live leaderboards, and compete
          across tournaments like The Masters and beyond.
        </p>

        <div className="mt-10 flex gap-4">
          <button className="bg-white text-black px-6 py-3 rounded-lg font-medium">
            Create a Pool
          </button>
          <button className="border border-white/20 px-6 py-3 rounded-lg">
            Learn More
          </button>
        </div>
      </section>

      {/* FEATURES */}
      <section className="grid md:grid-cols-3 gap-8 px-10 pb-20">
        <div className="border border-white/10 p-6 rounded-xl">
          <h3 className="text-lg font-semibold">Private Pools</h3>
          <p className="text-gray-400 mt-2 text-sm">
            Create pools with friends and control entry, rules, and payouts.
          </p>
        </div>

        <div className="border border-white/10 p-6 rounded-xl">
          <h3 className="text-lg font-semibold">Live Leaderboards</h3>
          <p className="text-gray-400 mt-2 text-sm">
            Track performance in real time with dynamic scoring updates.
          </p>
        </div>

        <div className="border border-white/10 p-6 rounded-xl">
          <h3 className="text-lg font-semibold">Multi-Sport Ready</h3>
          <p className="text-gray-400 mt-2 text-sm">
            Expand beyond golf into March Madness, NFL playoffs, and more.
          </p>
        </div>
      </section>

    </main>
  );
}